[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python3 -u proxy_server.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[deploy.envs]
PROXY_PORT = "${{PORT}}"
KEEP_ALIVE = "true"
